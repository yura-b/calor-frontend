import React from 'react';

interface Props {
  orderData?: any;
}

const HistoryOrder: React.FC<Props> = ({ orderData }): React.ReactElement => {
  return <div className="">History order</div>;
};

export default HistoryOrder;
